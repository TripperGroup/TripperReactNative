import { DefaultTheme, DarkTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: 'white',
    accent: '#67B75F',
  },
};

export const colors = {
  primary: 'white',
  accent: '#67B75F',
};
